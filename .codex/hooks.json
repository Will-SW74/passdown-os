{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "printf '0' > sessions/.toolcount 2>/dev/null; echo '=== passdown-os 交接內容（SessionStart hook）==='; cat handoff/CURRENT.md 2>/dev/null || echo '（警告：找不到 handoff/CURRENT.md）'; echo '--- 以上即 CURRENT.md 全文；請依 CONSTITUTION.md 執行 Session 開始協定其餘步驟。'",
            "commandWindows": "python -c \"import sys; from pathlib import Path; sys.stdout.reconfigure(encoding='utf-8'); p=Path('sessions/.toolcount'); p.write_text('0', encoding='ascii'); h=Path('handoff/CURRENT.md'); print('=== passdown-os 交接內容（SessionStart hook）==='); print(h.read_text(encoding='utf-8') if h.exists() else '（警告：找不到 handoff/CURRENT.md）'); print('--- 以上即 CURRENT.md 全文；請依 CONSTITUTION.md 執行 Session 開始協定其餘步驟。')\"",
            "timeout": 10,
            "statusMessage": "載入 Passdown OS 交接內容"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "sh entrypoints/hooks/checkpoint-counter.sh --json",
            "commandWindows": "python -c \"import pathlib, shutil, subprocess, sys; g=pathlib.Path(shutil.which('git') or ''); s=g.parent.parent/'bin'/'sh.exe'; sys.exit(subprocess.call([str(s), 'entrypoints/hooks/checkpoint-counter.sh', '--json']))\"",
            "timeout": 5,
            "statusMessage": "Passdown OS 檢查點計數"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 entrypoints/hooks/archive-codex-transcript.py",
            "commandWindows": "python entrypoints/hooks/archive-codex-transcript.py",
            "timeout": 30,
            "statusMessage": "保存 Codex 逐字稿快照"
          }
        ]
      }
    ]
  }
}
