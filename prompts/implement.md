# 交辦單：實作

- **指派給**：<subagent 或 agent 代號>（model: <查證後的實際值或「預設」> / effort: <medium 起跳>）

## 目標與動機

要做：<行為層面的描述，例如「`parseHeader` 遇到空字串時回傳 null 而不是 throw」>
因為：<這個改動解決什麼問題、屬於哪個 change/task>

## 範圍/邊界

- 只動：<允許修改的檔案/目錄，micro-task 原則上 1-2 個檔案>
- 不要動：<明確排除的範圍，例如「不改公開 API 簽名」「不升級依賴」>
- 起點：<從哪裡開始讀，精確到 `檔案:行號` 或符號名>
- 接手既有 task 時，先依 [`RUBRICS.md`](../RUBRICS.md) 第 6 節核對交接描述與目前 repo 狀態；前提不成立就回報，不照著過期結論修改。

## 驗收條件

- [ ] <行為改變的可檢查描述，例如「輸入空字串回傳 null，有對應測試」>
- [ ] <測試要求，例如「既有測試全綠 + 新增至少 1 個涵蓋此邊界的測試」>
- [ ] 實跑或測試輸出為證，不接受「看起來對」。

## 回報格式

1. 結論：驗收條件逐條核對（過/不過，附證據）。
2. 改動清單：`檔案:行號` — 改了什麼。
3. 未完成項與原因（沒有就寫「無」）；過程中發現的範圍外問題（只回報不處理）。
