# 本機記憶匯入暫存區 — 清洗完才進正規位置
imports/*
!imports/README.md
!imports/.gitkeep
.spectra/
openspec/.vector-search.db*

# Agent 個人本機設定——可能含權限、路徑或個人 hook，不進版控
.claude/settings.local.json

# session 執行期的本機暫存訊號（值班鎖、工具呼叫計數器）——不是記錄、不進版控
# 真正的記錄在 sessions/*.md；這兩個檔案是單機單次 session 的即時狀態
sessions/.active_lock
sessions/.toolcount
sessions/.active_session

# 逐字稿本機歸檔區——含未清洗敏感資訊，絕不入版控；可攜靠雲端/USB 同步資料夾
transcripts/*
!transcripts/README.md
!transcripts/.gitkeep
