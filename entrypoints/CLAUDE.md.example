<!-- 
  passdown-os 入口段落 — Claude Code (cc) 用
  使用方式：將下方 ## Passdown OS 段落加入專案根目錄的 CLAUDE.md
  若 CLAUDE.md 已有其他內容，將此段落附加到末尾即可
-->

## Passdown OS

本專案使用 `passdown-os/` 框架管理跨 AI Agent 協作（codex / cc / agy 三者平權交接）。

**每次新 session 開始時，依序執行：**

1. 讀 [`passdown-os/CONSTITUTION.md`](passdown-os/CONSTITUTION.md) — 最高規則來源（每次對話只需讀一次）。
2. 讀 [`passdown-os/handoff/CURRENT.md`](passdown-os/handoff/CURRENT.md) — 目前在哪、做到哪、下一步是什麼。
3. 依 CONSTITUTION.md「Session 開始協定」的後續步驟繼續。

**結束 session 前，必須完整執行 CONSTITUTION.md 中的「Session 結束協定」，不可省略。**

**語言與編碼（一啟動即生效）**：回覆與所有交接文件一律正體中文（zh-TW）；程式碼一律必須加上解釋 Why 的正體中文註解；寫入檔案一律 UTF-8。詳見 CONSTITUTION.md 第 10、11 節。

### 調度模式（需已安裝 `.claude/agents/` 的 subagent 範本組，見 `passdown-os/entrypoints/claude-agents/README.md`）

每個 task 開始動工前，先確定本次的調度模式。使用者已在任務描述中指明 → 直接採用，不要再問；未指明 → 用一個問題問使用者（只問這一次）；**無人值守／headless 執行（無法向使用者提問）時不阻塞等待，直接預設用分派模式**：

1. **分派模式（預設）**：依路由表把子任務派給對應 subagent，主對話只做拆解、決策、審查與交接——即 `passdown-os/DISPATCH.md` 的「指揮官不下場」。
2. **指揮官模式（強制自做）**：主對話全程自己做，不派任何 subagent。Context 紅線與 Session 協定照常適用——這個模式燒 context 更快，更要遵守 60%/70% 存檔線。

使用者回答「本 session 都用某模式」→ 整個 session 沿用，後續 task 不再問。

分派模式的路由表、派工要點（交辦單完整放進 subagent prompt）、升降級規則 → 依 `passdown-os/entrypoints/claude-agents/README.md` 與 `passdown-os/DISPATCH.md` 第 5 節執行。
