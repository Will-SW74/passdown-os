# Redaction Log

記錄從 `passdown-os/imports/`（本機原始記憶匯入）清洗時，移除了哪些敏感資訊。只增不改。

用法：每次匯入清洗後補一筆。

## 範例格式（可刪除，供參考）

### YYYY-MM-DD — 匯入來源 `<agent>` 本機記憶

- 移除項目：<例如 API key / 絕對路徑 / 個人帳號 / 不相關私人對話>
- 處理方式：<刪除該段 / 用佔位符取代>
- Agent: <codex|cc|agy>

目前尚無條目。
